# Swift / Xcode
.build/
.swiftpm/
DerivedData/
*.xcodeproj/
!*.xcodeproj/project.pbxproj
!*.xcodeproj/xcshareddata/
*.xcworkspace/
!*.xcworkspace/contents.xcworkspacedata
xcuserdata/
*.xcuserstate
Package.resolved

# macOS
.DS_Store

# Editors
.vscode/
.idea/

# Local secrets (never commit API keys)
.env
.env.*
secrets.json
# Secrets.swift is now a thin Keychain wrapper — safe to track. The legacy
# LittleAI path stays excluded so old worktrees don't accidentally surface it.
macos-app/Sources/LittleAI/Secrets.swift
macos-app/dist/
